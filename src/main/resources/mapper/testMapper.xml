<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="app.codingGround.api.test.Mapper.TestMapper">
    <select id="getMybatisTest" resultType="testVo">
        SELECT * FROM user WHERE USER_ID = #{userId}
    </select>

    <select id="getMyRanking" resultType="UserInfoVo">
        SELECT  FROM user WHERE USER_ID = #{userId}
    </select>





</mapper>
